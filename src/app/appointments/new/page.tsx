// page to add new appointments - for all appts view, use src/app/appointments/page.tsx
// will need a way to route back to all appointments
// TODO: Phase 3 of the appointment plan — wire ContentInput → ExtractedDataReview → AppointmentForm,
// and route back to /appointments on save.
"use client";

export default function NewAppointmentPage() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-semibold">Add Appointment</h1>
      <p className="mt-2 text-slate-600">Coming soon.</p>
    </main>
  );
}
