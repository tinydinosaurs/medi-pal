/* ContentInput wrapper that with "add manually" option. 
Both file drop and add manually trigger the rendering of AppointmentReview,
which is an editable form. Users can input full details, or add missing
details after a file drop.
*/

/* pseudocode and comments 

import React, { useState } from 'react';
import {ContentInput} from '../ContentInput';

export default const AppointmentInput = () => {

// render ContentInput with options for file drop or add manually
*/
